package com.example.lohonotheme.changeTheme

/**
 * we just defined this connector for future purpose
 * to make vision of connection between ui and library
 */
interface ChangeThemeConnector {
    fun changedToDark()
    fun changedToLight()
}